<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edwin.proyecto_final">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_calculer"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_calculer"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Presentacion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
            <activity android:name=".Tab1"></activity>

            <meta-data
                android:name="preloaded_fonts"
                android:resource="@array/preloaded_fonts" />

            <activity android:name=".Tab2"></activity>
            <activity android:name=".Tab3"></activity>
            <activity android:name=".Tab4"></activity>
            <activity android:name=".Favorito"></activity>
         <activity android:name=".MainActivity"></activity>


    </application>

</manifest>